# Provider hosts for ZFS encryption password storage
# One host per line. Lines starting with # are comments.
# Passwords are fetched from ALL reachable hosts and deduplicated.

192.168.28.2
192.168.28.3
